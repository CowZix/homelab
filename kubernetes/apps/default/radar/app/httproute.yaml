apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radar
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https

  hostnames:
    - radar.onmyputer.com

  rules:
    - backendRefs:
        - name: radar
          port: 9280